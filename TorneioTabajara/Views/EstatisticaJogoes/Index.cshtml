@model IEnumerable<TorneioTabajara.Models.EstatisticaJogo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Partida.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlacarTime1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlacarTime2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Partida.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlacarTime1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlacarTime2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PartidaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PartidaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PartidaId })
        </td>
    </tr>
}

</table>
